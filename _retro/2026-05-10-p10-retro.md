# P10 Retro — 2026-05-10 (sanitized public companion)

**Cadence:** weekly P10 retrospective; previous retro 2026-05-03 (7 days, on-cadence).
**Window:** 2026-05-03 → 2026-05-10 (covers the v3.10.x ship cycle: v3.10 → v3.10.4).
**Mode:** Full 8-step ritual + slack-channel-monitor sub-system ship-then-kill arc as own section.

> **Public-companion note:** this is the sanitized public narrative for the 2026-05-10 interactive P10 retro. The canonical narrative is private (`_retro/2026-05-10-p10-retro.md` in the operator's workspace). Stakeholder names are dropped, workspace-specific paths are genericized to governance paths only, and `detail_ref` cross-pointers to private files are stripped. The YAML sidecar at `retros/2026-05-10-p5.yaml` is the structured surface for the dashboard renderer. **First parallel public narrative for an interactive P10 retro** — prior public-side cycles (P3 / P4) shipped sidecar-only, with public `_retro/` containing only automated SessionStart-hook drafts.

---

## 1. Catchment shape

**167 entries across 7 days** in 10 workspace CHANGELOGs. Catchment-density correlates with sub-system saturation (5 distinct sub-systems shipped this week), not problem-density. The promotion candidates surfaced below are real, but the *shape* of this retro is "ship-pace audit" not "bug-rate audit."

| Arc | Date | Density | Status |
|---|---|---|---|
| v3.9.x close + previous P10 inheritance | 2026-05-03 | 9 root entries | Closed |
| Council-deck v0.1 build day | 2026-05-04 | 7 root + 14 council | Closed |
| Council-deck reworked sample-slides + walkthrough prep | 2026-05-05 | 21 root + 23 council | Closed |
| **Tier 1 ship-then-kill + Phase 2 Luma framing** | 2026-05-06 | 14 root + 19 monitor | Closed (own §3) |
| Council meeting + deck script v2.x | 2026-05-07 | 12 cross-workspace | Closed |
| **Triage-tool build → ship + folder-convention rule** | 2026-05-08 | 7 root + 18 council + 4 cross-ws | Closed |
| **Sumi v1.0 / v1.1 / v1.2 trilogy** | 2026-05-09 | 14 root | Closed |

---

## 2. What shipped well

- **Sumi trilogy n=3 sub-ship pattern in one day** (doctrine → build → governance, 3 commits each, 3× Saturday). Per `now.md` watch-list: n=4 threshold reaches canonical-promotion eligibility if v2.x stakeholders-class follows the same shape.
- **Triage-tool end-to-end in 1 day**: design via `superpowers:brainstorming` → plan via `superpowers:writing-plans` → 6-task build via `superpowers:subagent-driven-development` → real-data smoke PASS via `--bypass-auth-tier` → council report shipped to thread. Arc shape repeatable.
- **Tier 1 ship-then-kill within hours**: clean exercise in eyeball-rejection authority. Tier 2 LLM summarization redesign opens fresh next session under Luma consult upfront. Discipline win — the controller killed their own ship when eyeball rejected the metadata-only digest mid-stride; Tier 2 then shipped same-day with classifier + sentinel + sample + integration + hook + format-fix; first morning-fire under flipped trigger lands Mon 2026-05-11.
- **n=5 substring-vs-structure family** — promoted at test-layer in v3.9.1 (output-checklist §4 sub-rule c); cross-layer §18 promotion shipped this retro (see §4 below).

---

## 3. Tier 1 ship-then-kill arc

**Timeline 2026-05-06:**
- Morning: scaffold + classifier TDD + channels.yaml + digest builder TDD + Slack client + LaunchAgent install
- Mid-day: Tier 1 PILOT SHIP — 12/12 tasks closed; Codex P3B verdict folded (P1 window-drift fix anchored to scheduled fire instead of runtime); live-fire DM verified
- Evening: controller eyeball-rejected the metadata-only digest as insufficient for daily inbox-zero; **TIER 1 KILLED** — `launchctl unload` + LaunchAgent removed; tomorrow's morning fire cancelled
- Late-night: Tier 2 design + ship same day — sentinel + sample + integration + hook + format-fix; Phase 2 Luma framing sketch authored

**Process learning (n=1):** *eyeball-rejection authority — the controller can kill their own ship within hours of completion if the artifact doesn't meet the lived-experience bar.* The 12/12 task completion + Codex P3B verdict fold is the "build is correct" signal; metadata-only digest is the "build doesn't solve the problem" signal. Two independent quality gates; eyeball wins on user-experience axis. Architectural learning: design-spec acceptance test should include a paste/use eyeball check pre-LaunchAgent-install, not post.

**Doctrine candidate:** Promote eyeball-rejection authority to a §4 sub-bullet at next P10 if recurrence (n=2). Currently watching.

---

## 4. Recurring quality gaps + promotions

### 4a. Promoted this retro (executed)

- **§18 "Matcher discipline — structural over substring"** added to `_config/output-checklist.md`. Cross-layer n=5 evidence (validator + hook + audit-test + test-harness paths + bytes-vs-chars slicing). Sibling family (case-sensitive-grep-as-hidden-contract n=4) cross-referenced. Pairs with §4 sub-rule c at test-side.
- **§19 "Reading-bundle rendering" + §20 "Long-lived branch merge discipline"** added to `_config/output-checklist.md`. §19 codifies the MD-vs-HTML deliverable doctrine landed Phase 1 (Luma F3 edit-locus reframe; output-side mirror of v3.5 synthesis-surface pre-render pattern). §20 codifies the n=1 long-lived branch state-file conflict pattern at doctrine-only tier per Teacher recommendation; n=2 trigger documented for hook-side enforcement.
- **§15(c) classification-check extension** — when placing a new agent in publication-shape README content, derive classification from the README's own taxonomy axis (currently invocation-shape: conversational-rail vs specialized-validator), NOT from descriptors used in internal CONTEXT prose. Catalyst: Sumi public agent card initially placed in Strategic Layer anchored on "5th P3 enforcement layer" descriptor; corrected to The Crew (sibling-shape to Codex P3B). Sibling pattern to catalyst-vs-domain conflation (n=2 confirmed across distinct surfaces today).

### 4b. Pending controller's gate (in `.claude/teacher-proposals.md` as Status: pending)

- **Aggressive MUTATION_TOOLS Teacher proposal** — `mcp__*-write` tool inclusion in the MUTATION_TOOLS frozenset. Two direction options pre-staged: (2) wildcard regex `__write|__create_|__update_|__delete_|__edit_|__send_|__reply|__push_|__merge_` against ~80–90% of currently-installed MCP write tools; (3) explicit allowlist enumerated across ~70+ tools. YAML meta-proposal composability matrix documented. Execution-trigger: SDK MCP tool surface stabilization (Anthropic plugin release notes mention naming convention standardization, OR >2 weeks pass without new MCP tools landing with non-conforming verb shapes, OR a near-miss case where MCP-mutation skipped bar discipline). Re-evaluation cadence: next 2 P10 retros (5/17, 5/24).
- **Briefing-card passthrough miss family** — REJECTED via novel doctrine tag "axis-coherent, evidence-weight-rejected" (n=4 outside-retro re-open watch). Distinct from axis-rejected + locus-disputed. Surfaced during Phase 1 doctrine ship gate dispositions.

### 4c. Deferred (n<threshold or workaround already shipped)

- Focus-chain aspirational-write surrogate (n=1; candidate §14 sub-family #11 if n=2)
- P5 ↔ worktree-cycle orphan-edit drift (n=1; PreToolUse hook becomes mandatory at n=2)
- Subagent silent-stall pattern (n=2 distinct surfaces; weak forward-rule strength; defer to n=3)
- GOOGLE_CREDENTIALS_JSON cross-workspace stale-env-var (n=2; mechanical fix shipped per-workspace; family doctrine weak — defer to n=3)
- Slot-vs-identity edge case in cascade rename (n=1; narrow-surface)
- Plan scope gap on `.py` hooks + `_config/scripts/` (n=1; addressable via plan-template extension at n=2)

---

## 5. Sumi trilogy + v1.3 drift-scan ship narrative

The single largest architectural ship of the cycle — Sumi joining the fam as the 5th P3 enforcement rail, in 24 hours across three sub-shipped versions (v1.0 / v1.1 / v1.2) plus the v1.3 drift-scan extension landed the next day.

**v1.0 morning (2026-05-09):** subagent-output-relay rubrics + Phase D 4-failure descope + Tools=[Read] minimum-viable profile + governance ship v3.9.3 → v3.10. Sumi placed under P3 enforcement layer (Frame E lock, NOT a new Pn — preserves existing protocol slot taxonomy). Symlink-canonical write-side rule promoted at n=1 from Phase E friction (4 tool-rounds before convergence): edits go to the canonical filename, not the symlink; read-permission is per literal `file_path` string, not per inode.

**v1.1 night (2026-05-09):** design-spec output_type + cross-check resolution + Tier B Read+Grep tool grant + governance ship v3.10 → v3.10.1.

**v1.2 night (2026-05-09):** external-bound paste-text output_type + stakeholders.council activation + additive-overlay rubric composition + governance ship v3.10.1 → v3.10.2. Sumi v1.2 catches the original n=1 attribution-conflation catalyst that motivated her existence — closing the catalyst lineage loop.

**v1.3 mid-day (2026-05-10):** drift-scan invocation mode added. Walks every active rubric in `_config/sumi-rubrics.yaml` and verifies each anchor still resolves in its declared source file. New `drift_findings` verdict shape with `drift_class` enum (`anchor_not_found` / `section_not_found` / `source_file_unreadable`). Combined with MUTATION_TOOLS Conservative extension as v3.10.4 unified gate.

**Process learning n=1:** Sumi-class governance-ship sub-ship pattern (3 commits per sub-ship: doctrine → build → governance) held across all three v1.x sub-ships in one day. Eligible canonical-spec template at n=4 — the v2.x stakeholders-class follow-up will tip it.

**Codex P3B cross-find n=1:** the v3.10.4 drift-scan hook adversarial review surfaced an `additionalContext` envelope-shape bug; the same bug pre-existed in `sumi-post-task.sh` (the architectural template the new hook was modeled on) since v3.10 ship 2026-05-09. Parity-fix landed in the same patch. Rule (n=1, watch n=2): when adversarial review of a NEW hook surfaces a structural finding (envelope shape, fail-OPEN gating, security routing), explicitly sweep the architectural-template sibling for the same pattern in the same review session.

---

## 6. Cross-cutting findings (process-level)

### 6a. Retro-session-as-datapoint-source meta-pattern (n=2)

2nd retro in a row catching n+1 datapoint *during the retro session itself* (5/03 + 5/10). Today: response 1 missed briefing-card passthrough (added n=3 to family A); responses 1+2 missed checkpoint bar on tool-burst turns (added n+4 to family B as new sub-axis "missed-on-tool-burst-turn"). The retro itself runs the same response-gate contracts under synthesis-heavy load; render-side + emission-side fixes both survive this; doctrine-only fixes don't. v3.10.3 patch-pair (Shape 2 render-side briefing banner + Shape 3 PostToolUse bar-format validator) was the structural fix.

### 6b. Subagent Write-permission Class-S signal — ~~RESOLVED at config layer~~ ❌ **[CORRECTED 2026-07-15] NOT RESOLVED**

> ❌ **[CORRECTED 2026-07-15] ** The permission fix this cites never worked. Every rule in the settings allowlist used a single-leading-slash absolute path, which Claude Code anchors at the settings source rather than the filesystem root, so the rules resolved to paths that cannot exist. Both the 2026-05-10 Write rules and the 2026-05-17 Edit rules were inert; subagent grants were dead from 2026-05-10 to 2026-07-15 (66 days). The failure was invisible because a dead allow-rule does not error, it degrades to a permission prompt approved by hand. Corrected and verified 2026-07-15 by a three-arm probe (granted path allowed / ungranted control denied / counterfactual single-slash denied). The signal was NOT resolved here and stayed open for 66 days. Retained verbatim below — the correction is more instructive than a clean record.

Both Teacher AND Luma reported Write denied at canonical persistence surfaces this retro session (n=2 in single session). Investigation closed: subagents inherit parent session's `permissions.allow` rules at runtime per Claude Code docs; `permissionMode` in agent frontmatter is documented but ignored under parent-mode precedence. Fix shipped: `permissions.allow` block added to root `.claude/settings.json` with path-targeted Write rules per subagent contract (Teacher → proposals + insights-archive + input-archive moves; Luma → designs/). Verification subagent PASS. Class-S signal closed at every layer (config + doctrine + production).

### 6c. Layer-classification scope-conflation pattern (n=1, watch n=2)

Catalyst: Sumi public agent card placement in README. Initial draft anchored on "5th P3 enforcement layer" descriptor from internal CONTEXT prose and unconsciously promoted that to a Strategic Layer claim — wrong taxonomy axis. Strategic Layer = direct conversational rails the controller talks to in real time (Root / Breakline / Luma); Sumi is sibling-shape to Codex P3B (specialized validator triggered for narrow scope) which lives in The Crew. Two semantically different uses of "layer" (enforcement-tier vs invocation-shape) got conflated. Sibling pattern to catalyst-vs-domain conflation (also n=2 today across distinct surfaces — promotion-watch class). Rule installed at output-checklist §15(c).

### 6d. Teacher autonomy / bypass-Root-invocation question — DEFERRED to next P10

Three sub-options surfaced: (a) SessionStart hook reminder when "days-since-last-Teacher" > N (reminder, not bypass); (b) cron-fired Teacher via headless Claude Code mirror of monitor architecture (full bypass); (c) Class-S autonomous escalation extension to Teacher's contract (partial bypass). Bypass options (b) + (c) required subagent Write-permission gap to be resolved first — that's now done (§6b). Luma-territory at next P10 retro — multiple direction options, OS-governance scope.

---

## 7. Watch-list for next P10 retro (2026-05-17)

- **n=4 threshold for Sumi-class governance-ship sub-ship pattern** (currently n=3 v1.0/v1.1/v1.2; v2.x stakeholders-class would tip to canonical promotion)
- **Eyeball-rejection authority pattern** (n=1 today — Tier 1 ship-then-kill); promote to §4 sub-bullet at n=2
- **Retro-session-as-datapoint-source meta-pattern** (n=2; needs structural fix — currently relying on Shape 2+3 v3.10.3 patch to break the chain)
- **Buffer-to-retro-candidates curation gap** — does it persist? Did Teacher's direct-from-buffer synthesis prove out?
- **Aggressive MUTATION_TOOLS proposal execution trigger** — has SDK MCP tool surface stabilized? Re-evaluate at this retro and 5/24.
- **Layer-classification scope-conflation pattern** — n=2 catalyst surfaces? Promote to canonical rule if so.
- **Codex P3B sibling-sweep discipline** — n=2 catalyst surfaces (next hook ship modeled on a sibling)?
- **Teacher autonomy decision** — Luma framing landed?
- **Long-lived sprint branches conflict** — n=2 surfaced on next worktree-based ship?
- **Public sidecar metric backfill** — has the per-cycle tally script shipped so discipline_metrics + latency_observations + fam_dispatch_distribution can fill in cross-cycle apples-to-apples?

---

**Retro authored by:** Root (P10-auto invocation, P10 retro 2026-05-10)
**Subagent dispatches:** Teacher + Luma (both reported Write denied on first invocation; Class-S investigation + config-layer fix shipped same session; Root persisted both outputs verbatim per output-checklist §6 sub-bullet during the gap)
**Public companion authored:** 2026-05-10 mid-day (sub-ship 3 of Phase 3 retroactive public repo + dashboard sync)
